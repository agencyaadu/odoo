[options]
; This is the password that allows database operations:
; admin_passwd = admin
; Leave DB settings empty so env/CLI can override for external Postgres.
db_host =
db_port =
db_user =
db_password =
db_sslmode =
;addons_path = /usr/lib/python3/dist-packages/odoo/addons
default_productivity_apps = True
